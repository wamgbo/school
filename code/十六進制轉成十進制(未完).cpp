#include <iostream>
#include <string.h>
#include <math.h>
using namespace std;
int main(int argc, char** argv) {
	int i,point;
	float b,c;
	string a;
	cin>>a;
	for(i=0;i<=a.size()-1;i++){
		if(a[i]='.'){
			point = i;
		}
	for(i=0;i<=a.size()/2;i++){
		if(a[i]>a[point]){
			
		} 
	}
	
	} 
	cout<<point;
	return 0;
}
